#include "main.h"
#include <stdio.h>

/**
 * add - adds two integer
 * @a: the first integer
 * @b: the second integer
 * Return: the result as integer
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);

}
