#include "main.h"
#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * _isdigit - check if character is a digit
 * @c: character to be checked
 * Return: character
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
