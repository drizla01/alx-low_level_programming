#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - determines if input is a letter
 * @c : character
 * Return : int
 */

int _isalpha(int c)
{
	return (isalpha(c));
}
