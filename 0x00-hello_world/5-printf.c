#include <stdio.h>

/**
 * main - prints text with proper grammar,
 * but the outcome is a piece of art,
 * Return: 0 if executed correctly, otherwise reutrns nonzero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
